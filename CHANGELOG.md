# Changelog

## 1.6.1 (2026-08-30)
- Description refreshed for the 1.6.0 trigger surface: options, tokenized stocks, LP positions, trade anatomy, curator comparisons, fee rails, crowding, and Terms-of-Service monitoring now fire the skill. Routing re-verified: 16 of 16 test queries, including five should-not-trigger traps.

## 1.6.0 (2026-08-30)
Major content release: three new references (options, trade anatomy, curation frameworks rewritten), the 2026 OTC-book failure class with three verified prints, the curator/allocator landscape across eleven published frameworks, and ordering-layer economics across five chains. Sources: six primary documents read in full (including a 33-page research report), 20+ documentation sites fetched, key claims verified against independent reporting and keyless APIs (DefiLlama prints dated Aug 30 2026). Research digests live in the private repo one level up (research/2026-08-30-options-otc-curators/).

- NEW references/options-and-liquidity.md: options from zero for non-finance readers (calls, puts, strikes, premium, ITM, intrinsic vs time value, implied vs realized volatility, the Greeks, covered calls, cash-secured puts); the core identity that a concentrated liquidity position IS a short options position (fees = premium paid by flow, LVR = the seller's losses, vega unpaid); the two onchain market structures (Panoptic's perpetual streaming-premium options on Uniswap chunks vs Derive's self-custodial CLOB, dated TVL prints); Uniswap v4 hooks as pool admin keys; the tokenized-stock/memecoin pair as the toxic limit case with the option-writer's weekend gap; STORMM's pre-launch claims flagged against the mechanics.
- NEW references/trade-anatomy.md: the order-type vocabulary (market, limit, post-only, reduce-only, time in force, stop and take-profit triggering on the venue MARK, TWAP execution vs TWAP oracle, chase, atomic baskets, fat-finger bands, SNARK-verified matching); the trade roster under the organizing question WHAT IS THIS BOOK SHORT; the discounted locked-token OTC trade as centerpiece with three verified 2026 prints (LAB sell side; PiggyBank's disclosed loss with honest side-pocket treatment; Neutrl's protocol-scale halt) and the discount decomposed like a yield (cliff, hedge carry, adverse selection, mark risk); the Terms-of-Service tell (exit clause amended days before the gate) as a monitoring surface; NAV-under-illiquidity mechanics; the pre-funded-short-leg check (Ethena's reserve fund, current 0% ongoing contribution noted as a parameter to check); the funded-vs-borrowed crowding overlay; LTCM stated precisely (Fed-brokered private recapitalization, 1998).
- REWRITTEN references/curation-frameworks.md: curator and allocator as FUNCTIONS held per product, with the programmatic allocator (Spark Liquidity Layer's rate-limit architecture) as the automated form; the convergent ten-stage anatomy across eleven published frameworks (Clearstar, Steakhouse, Gauntlet, MEV Capital, KPK, Yearn, Armitage, Birch Hill, Noon, K3, Spark); differentiators as discriminating questions (published thresholds vs adjectives, severity-not-probability scoring, observability gates, microstructure inputs, insurance gating, absence-of-process-as-finding); self-dealing, fee-stacking, and proprietary-book conflict checks; the verification asymmetry, with KPK's pre-halt Tier C on a failed synthetic receipt as the genre's calibration print.
- Concepts 8: the ordering layer decomposed: base fee vs priority fee, and the four routes ordering rent takes by design: BURNED to holders (Ethereum), AUCTIONED to validators (tips plus proposer-builder separation; Solana's 50/50 base-fee split with priority fees 100% to the leader), OPERATED as sequencer margin (Base, MegaETH: fees minus data-availability costs, accruing to the company), or SUPPRESSED BY POLICY (Monad's delegation rules against centralized order flow and third-party builders). Monad's gas-limit-charged quirk noted. TradFi anchor: rails charge tolls; ask who receives the toll.
- Concepts 4: discounts decompose like yields. Concepts 9: two new failure shapes (strategy drift; hedged-locked-inventory whose funding leg the counterparty can weaponize). Concepts 12: founder-signed audits are disclosures, not assurances; allocators publishing curator reviews rewarded. Concepts 16: pointer to the options file. Tokens: OTC/loan menus and unilateral vesting changes as observable extraction evidence; the funded-vs-borrowed five-test scorecard (attributed, author-holdings disclosure noted). Credit-cycles: borrowed crowding as the Ponzi-financed bid. Market-pulse: recovery quality beats drawdown depth; calm-vs-crash correlation; legal-terms diffing; OTC discount chatter. Perps: verifiable-matching note. Checklist: new Strategy Products section plus curator-role and kink-sizing rows. Analogs: five new rows (PIPE and restricted stock, side pockets and gates, options market maker, exchange priority and payment for order flow, program baskets).
- Glossary merged into one alphabetical list, 114 entries (~30 added: options, execution, and structure vocabulary, plus the MEV Capital disambiguation). Manifest: 115 sources, adding Panoptic, Lighter, Neutrl, PiggyBank, KPK, MEV Capital, Armitage, Birch Hill, Noon, K3, StonkBrokers/Robinhood Chain, and a new chains category (Base, Monad, MegaETH, Solana fee and architecture docs); pages added to Ethena, Spark, Yearn, Steakhouse, Derive. Evals: two new cases (OTC delta-neutral assessment; tokenized-stock/memecoin LP), 8 total.

## Unreleased notes merged into 1.6.0 (kept for history)
- New references/curation-frameworks.md: the anatomy of a professional curation process (ten stages, three phases), distilled and generalized from Clearstar's published Capital Allocation Framework (public Notion site, retrieved Aug 30 2026): disqualification gates with no override, asymmetric weighting of weakness, tier-linked allocation caps with downgrade clocks, engagement loops, call-surface-only whitelisting, exploit-wired monitoring (automation only where speed beats judgment), dual sign-off execution, and the agent-as-tool accountability pattern (never ship raw agent output; a named human signs). Wired into SKILL.md routing, concepts 3, playbook step 11, three checklist rows. Clearstar added to the manifest (risk-research, should).
- Regression fix: 1.5.3's panel flipped the verified Resolv low print from $0.0025 to $0.025; primary sources (The Block, cited in Halborn's postmortem) confirm $0.0025. Restored with the source anchored in-text. Lesson recorded in CLAUDE.md: calibration prints only change with a primary source linked in the edit.
- Manifest hygiene: an en dash had been reintroduced in the how_to_use fetch-cap line; restored to "4 to 6 fetches".
- Still to land in this batch: options and concentrated liquidity reference; trade anatomy reference (basis, carry, OTC, arbitrage types and their failure points). Version flips to 1.6.0 when the batch completes.

## 1.5.6 (2026-08-28)
- Discovery layer added to data-sources: Portals Explorer as the reference terminal (find candidates, then assess the underlying venue; APY cards are mixes, Verified means claimed, trust ranks are not risk ratings, zap routers are an extra contract layer), with Summer.fi, vfat.tools, and APY.vision named on the same shelf. Manifest rows: Portals and Enso (both flagged execution-adjacent, read-only fences on the rows).
- CoinGecko keyless tier added to recipes: spot prices and 24h change, the denomination leg of realization-filter checks.

## 1.5.5 (2026-08-28)
- Staying-current protocol upgraded to a per-session check with ephemeral freshness: agents may follow the newer canonical files from a scratch copy for the session, but installing updates stays a user action, and the skill never overwrites its own files. Self-modification is explicitly forbidden by design: a skill that silently rewrites itself cannot be reviewed.

## 1.5.4 (2026-08-28)
- ve-model forfeiture rule (field repair): staked LPs forfeit swap fees to voters and earn only emissions; "fees + emissions" dashboard APRs are a menu of two seats, never a sum. Reframes every Aerodrome-style print.
- GeckoTerminal added to keyless recipes: pool-level volume, fees, and depth; the fastest check on whether an LP fee APR is backed by real volume.
- Output formatting rule: comparison tables for options and seats, tables or labeled lines for decompositions, calendars, and risk:reward arithmetic; prose only where reasoning needs sentences.

## 1.5.3 (2026-08-28)
Adversarial four-model review of everything added since release; all confirmed findings fixed.
- Finance corrections in the teaching layer: CCP default waterfall order (defaulter margin, defaulter fund contribution, CCP skin-in-the-game, THEN the mutualized fund) and the honest ADL contrast (depth and frequency, not presence); T-bills are discount securities, not par claims; sUSDS is an administered rate on a mixed book, not a T-bill passthrough; looping mapped to margin leverage and rehypothecation to collateral-as-liquidity; duration example recalibrated to the real liquidation print; nightly (not just weekend) equity-oracle gaps; settlement simplified to T+1.
- Concepts: Resolv depeg low corrected ($0.025, was off 10x); underwritten credit split into unsecured (3Jane-style) vs negotiated overcollateralized institutional (Maple-style); PT linear-discount oracle split into pure-curve (immune to squeezes, converts deterioration into protocol bad debt) vs min(curve, TWAP) (not immune); IL vs LVR single-benchmark rule; Kelp print now carries its source URL.
- Recipes: working vaultV2s query shipped and live-verified (flat fields, netApy exists); Merkl pagination (items=, count endpoint, name filter); rwa.xyz app and DeBank marked browser-tool-only; curator field returns an address, noted.
- Tooling: verify_manifest.py now checks llms_full and pages[], content-checks llms.txt (empty or HTML bodies fail), scans all prose files for em AND en dashes, requires skill_use per source, and validates README/CONTRIBUTING file links; PR-check workflow added (runs on every pull request); monthly workflow scoped with add-paths so automation can only touch manifest.json and maintenance-reports/; MAINTENANCE.md description matched to reality; Mellow llms.txt removed (empty body).

## 1.5.2 (2026-08-28)
- Morpho GraphQL recipe fixed: `vaults` returns only Vault V1 (MetaMorpho); Vault V2 strategy vaults live under the separate `vaultV2s` query. The recipe now requires querying both and cross-checking against DefiLlama. Found in field use: an agent following the old recipe missed every V2 book.
- Merkl recipe: compare campaigns by USD daily-reward fields, never raw token amounts (decimal normalization), and report tranche structure plus renewal history, not just an end date.

## 1.5.1 (2026-08-28)

Initial public release. Eval-gated before publish: paired with-skill vs no-skill runs across assessment, allocation, content, and execution-refusal cases; a look-through regression gate; adversarial audits by a panel of independent models; and live verification of every manifest source and data recipe.
